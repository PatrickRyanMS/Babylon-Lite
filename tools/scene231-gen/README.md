# scene231 test-asset generator

Generates `lab/public/test-assets/scene231-standard-features.glb` — a skinned + morphed + vertex-colored, UV-mapped tube exercising the new Standard-material features. See `docs/lite/scene231-standard-features.md`.

```n# one-time:
npm install @gltf-transform/core
node gen.mjs
```n
